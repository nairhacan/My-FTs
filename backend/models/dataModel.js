
//syn Models

// const db = require("../services/db");

// function getAll() { return db.all("SELECT * FROM items"); }
// function insert(item) { return db.run("INSERT INTO items (title, content) VALUES (?, ?)", [item.title, item.content]); }
// function update(item) { return db.run("UPDATE items SET title = ?, content = ? WHERE id = ?", [item.title, item.content, item.id]); }
// function remove(id) { return db.run("DELETE FROM items WHERE id = ?", [id]); }

// module.exports = { getAll, insert, update, remove };
